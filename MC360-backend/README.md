# MC360 Backend

Backend scaffold for MC360.

## Structure

- `src/` - application source
- `ml-models/` - Python ML microservice

## Run

1. Install dependencies: `npm install`
2. Start server: `npm start`
